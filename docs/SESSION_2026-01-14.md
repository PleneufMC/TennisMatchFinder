# 📅 Session de développement - 14 janvier 2026

## 🎯 Objectif de la session

**Sprint 4 : Réputation & Social** - Implémenter le système d'évaluation post-match pour construire la confiance au sein de la communauté.

---

## ✅ Travail accompli

### 1. Système de Réputation Post-Match ⭐

#### Concept
Après confirmation d'un match, le joueur peut évaluer son adversaire sur 3 critères :
- **Ponctualité** : Arrivée à l'heure, respect du planning
- **Fair-play** : Respect des règles, annonces honnêtes  
- **Convivialité** : Attitude positive, bonne ambiance

#### Base de données

**Table `match_ratings`** :
| Colonne | Type | Description |
|---------|------|-------------|
| id | UUID | Identifiant unique |
| match_id | UUID FK | Match concerné |
| rater_id | UUID FK | Joueur qui évalue |
| rated_player_id | UUID FK | Joueur évalué |
| punctuality | INT (1-5) | Note ponctualité |
| fair_play | INT (1-5) | Note fair-play |
| friendliness | INT (1-5) | Note convivialité |
| comment | TEXT | Commentaire optionnel (privé) |
| average_rating | DECIMAL(2,1) | Moyenne calculée |
| created_at | TIMESTAMP | Date de l'évaluation |

**Colonnes ajoutées à `players`** :
- `reputation_avg` : Moyenne générale
- `reputation_punctuality` : Moyenne ponctualité
- `reputation_fair_play` : Moyenne fair-play
- `reputation_friendliness` : Moyenne convivialité
- `reputation_count` : Nombre d'évaluations reçues

#### API

**POST /api/matches/[matchId]/rate**
```json
// Request
{
  "punctuality": 5,
  "fairPlay": 4,
  "friendliness": 5,
  "comment": "Très bon partenaire !"
}

// Response
{
  "success": true,
  "rating": {...},
  "message": "Merci ! Votre évaluation a été enregistrée.",
  "ratedPlayer": {
    "id": "...",
    "newAverage": "4.7",
    "totalRatings": 8
  }
}
```

**GET /api/matches/[matchId]/rate**
- Récupère les évaluations d'un match
- Indique si le joueur a déjà évalué (`hasRated`)
- Retourne l'évaluation reçue si disponible

---

### 2. Composants UI

#### RatingModal (`src/components/reputation/rating-modal.tsx`)
- Modal de saisie avec système d'étoiles interactif
- 3 critères avec description
- Affichage de la moyenne en temps réel
- Commentaire optionnel (500 caractères max)
- Animation de succès après envoi

#### ReputationBadge (`src/components/reputation/reputation-badge.tsx`)
- Badge affiché sur le profil avec note moyenne
- Tooltip avec détail des 3 critères
- Coloration selon la note (vert/jaune/rouge)
- Badge spécial "Partenaire Fiable" (⭐ 4.5+ avec 5+ avis)
- Version compacte pour les listes

---

### 3. Intégration dans le Flow

#### Confirmation de match (`match-confirm-form.tsx`)
1. Joueur confirme le résultat
2. Message de succès affiché
3. **Après 1.5s** → Modal d'évaluation apparaît
4. Joueur peut évaluer ou passer
5. Redirection vers /matchs

#### Page de profil (`profil/page.tsx`)
- Badge de réputation affiché à côté du rang ELO
- Détails visibles au survol (tooltip)

---

### 4. Badge "Partenaire Fiable" 🏅

**Conditions d'obtention** :
- Moyenne générale ≥ 4.5/5
- Au moins 5 évaluations reçues

**Caractéristiques** :
- Catégorie : Social
- Tier : Rare (bleu)
- Icône : UserCheck
- Couleur : Emerald (#10B981)

**Intégration** :
- Ajouté à `BADGE_DEFINITIONS` (17ème badge)
- Fonction `checkReliablePartner()` dans badge-checker
- Vérifié automatiquement après chaque nouvelle évaluation

---

## 📁 Fichiers créés/modifiés

### Nouveaux fichiers
```
src/
├── app/api/matches/[matchId]/rate/
│   └── route.ts                        # API évaluation
├── components/reputation/
│   ├── rating-modal.tsx                # Modal d'évaluation
│   └── reputation-badge.tsx            # Badge réputation
└── migrations/
    └── reputation-system.sql           # Script SQL
```

### Fichiers modifiés
```
src/
├── lib/db/
│   ├── schema.ts                       # Table match_ratings + colonnes players
│   └── queries.ts                      # Query getNewMembersToWelcome
├── lib/gamification/
│   ├── badges.ts                       # Badge "Partenaire Fiable"
│   └── badge-checker.ts                # checkReliablePartner()
├── lib/matching/
│   └── suggestion-engine.ts            # isNewMember + tag "Nouveau membre"
├── types/
│   └── player.ts                       # isNewMember property
├── components/matches/
│   └── match-confirm-form.tsx          # Intégration modal
└── app/(dashboard)/
    ├── matchs/confirmer/[matchId]/page.tsx  # Passage des infos adversaire
    ├── profil/page.tsx                 # Affichage ReputationBadge
    └── suggestions/page.tsx            # Section nouveaux membres
```

---

## 🗄️ Migration SQL à exécuter

```sql
-- Fichier: migrations/reputation-system.sql
-- Statut: ⏳ À exécuter sur Neon

-- 1. Créer la table match_ratings
CREATE TABLE match_ratings (...);

-- 2. Ajouter les colonnes à players
ALTER TABLE players 
ADD COLUMN reputation_avg DECIMAL(2,1),
ADD COLUMN reputation_punctuality DECIMAL(2,1),
ADD COLUMN reputation_fair_play DECIMAL(2,1),
ADD COLUMN reputation_friendliness DECIMAL(2,1),
ADD COLUMN reputation_count INTEGER DEFAULT 0;

-- 3. Ajouter le badge "Partenaire Fiable"
INSERT INTO badges (...) VALUES ('reliable-partner', ...);
```

---

### 5. "Nouveaux membres à accueillir" 👋

#### Concept
Mettre en avant les nouveaux joueurs du club pour les intégrer rapidement et encourager les anciens à les accueillir.

#### Critères d'identification
Un joueur est considéré comme "nouveau membre" si :
- **Moins de 3 matchs** joués
- **Inscrit depuis moins de 30 jours**

#### Implémentation

**Query `getNewMembersToWelcome`** (`queries.ts`) :
```typescript
// Récupère les nouveaux membres d'un club
export async function getNewMembersToWelcome(
  clubId: string,
  excludePlayerId?: string
): Promise<Player[]>
```

**Fonction `isNewMember`** (`suggestion-engine.ts`) :
```typescript
// Vérifie si un joueur est un nouveau membre
export function isNewMember(opponent: SuggestionPlayer): boolean
```

#### UI - Page Suggestions

**Section dédiée** (en haut de page) :
- Fond vert clair pour attirer l'attention
- Icône HandHeart + titre "Nouveaux membres à accueillir"
- Badge indiquant le nombre de nouveaux
- Liste compacte avec :
  - Avatar + Nom
  - ELO + Rang
  - "Inscrit il y a X jours"
  - Nombre de matchs joués
  - Bouton "Proposer"

**Tag dans les suggestions** :
- Les nouveaux membres ont le tag `"Nouveau membre 👋"` prioritaire
- Propriété `isNewMember: boolean` ajoutée à `SuggestedPlayer`

#### Lien avec le badge "Comité d'accueil"
- Badge existant : "Comité d'accueil" (Tier: Common)
- Critère : Être le premier adversaire de 5 nouveaux membres
- Cette feature facilite l'obtention du badge

---

### 6. Rappel d'inactivité (CRON Job) ⏰

#### Concept
Envoyer automatiquement une notification aux joueurs qui n'ont pas joué depuis 7 jours pour les encourager à revenir sur les courts.

#### Critères d'identification
Un joueur est considéré comme "inactif" si :
- **7+ jours** depuis le dernier match (`lastMatchAt`)
- **Membre actif** d'un club
- **Pas de notification** d'inactivité envoyée dans les 7 derniers jours

#### Implémentation

**Query `getInactivePlayers`** (`queries.ts`) :
```typescript
// Récupère les joueurs inactifs depuis X jours
export async function getInactivePlayers(
  inactiveDays: number = 7,
  excludeAlreadyNotified: boolean = true
): Promise<Player[]>
```

**Helper `createInactivityNotification`** (`queries.ts`) :
```typescript
// Crée une notification d'inactivité personnalisée
export async function createInactivityNotification(
  playerId: string,
  daysSinceLastMatch: number
): Promise<void>
```

#### API Endpoint

**POST /api/cron/inactivity-reminder**
- Authentification via `CRON_SECRET` (Bearer token)
- Récupère les joueurs inactifs
- Envoie les notifications
- Retourne les statistiques

**GET /api/cron/inactivity-reminder**
- Mode preview/test
- Retourne la liste des joueurs qui recevraient une notification
- Sans envoyer réellement les notifications

#### Netlify Scheduled Function

**Fichier** : `netlify/functions/inactivity-reminder.mts`
- **Schedule** : `0 10 * * *` (10h UTC = 11h heure française)
- **Fréquence** : Quotidienne
- Appelle l'API interne avec le CRON_SECRET

#### Message de notification
- **Type** : `inactivity_reminder`
- **Titre** : "🎾 On vous attend sur le court !"
- **Message** : Personnalisé selon la durée d'inactivité
  - 7-14 jours : "Cela fait un moment que vous n'avez pas joué..."
  - 14+ jours : "Cela fait plus de X jours que vous n'avez pas joué..."
- **Lien** : `/suggestions` (pour trouver un adversaire)

---

## 📊 Statistiques de la session

| Métrique | Valeur |
|----------|--------|
| Fichiers créés | 6 |
| Fichiers modifiés | 10 |
| Lignes de code ajoutées | ~1500 |
| Commits | 5 |
| Migrations SQL | 1 |
| CRON Jobs | 1 |

---

## 🔜 Prochaines étapes

### Sprint 4 ✅ TERMINÉ
- [x] ~~Système de réputation post-match~~ ✅
- [x] ~~Badge "Partenaire Fiable"~~ ✅
- [x] ~~"Nouveaux membres à accueillir"~~ ✅
- [x] ~~Rappel d'inactivité (CRON job)~~ ✅
- [ ] Afficher réputation dans la liste des joueurs (optionnel)

### Sprint 5 (Monétisation)
- [ ] Page `/pricing`
- [ ] Intégration Stripe (déjà configuré)
- [ ] Plans Premium/Pro

---

## 🔗 Commits de la session

1. `bb56895` - feat(reputation): implement post-match rating system - Sprint 4
2. `335fb9c` - docs: update roadmap and add SESSION_2026-01-14
3. `17611eb` - feat(suggestions): add 'New members to welcome' section - Sprint 4
4. `f9ed7e6` - docs: update SESSION_2026-01-14 with new members feature
5. `0f5a38c` - feat(notifications): add inactivity reminder CRON job - Sprint 4

---

## 📁 Fichiers créés (complet)

```
src/
├── app/api/
│   ├── matches/[matchId]/rate/route.ts     # API évaluation
│   └── cron/inactivity-reminder/route.ts   # API CRON inactivité
├── components/reputation/
│   ├── rating-modal.tsx                    # Modal d'évaluation
│   └── reputation-badge.tsx                # Badge réputation
migrations/
└── reputation-system.sql                   # Script SQL réputation
netlify/functions/
└── inactivity-reminder.mts                 # CRON Netlify
```

---

*Session terminée - 14 janvier 2026*
**Sprint 4 : Réputation & Social - TERMINÉ ✅**
